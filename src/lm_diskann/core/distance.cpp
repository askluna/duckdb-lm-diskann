/**
 * @file distance.cpp
 * @brief Implements distance calculation and vector conversion functions.
 *        (Implementations are now in distance.hpp)
 */
#include "distance.hpp"

// All function bodies have been moved to distance.hpp to make it a header-only style for these utilities.

namespace diskann {
namespace core {

// Implementations moved to distance.hpp

} // namespace core
} // namespace diskann
